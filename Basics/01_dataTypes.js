"use strict"; // we write this to tell the compiler that we are going to use newer syntax

// Primitive dataType
// Number
// Bigint
// string
// symbol=>used  for uniqueness 
// boolean
// null ==> when we used the value null it is a type of OBJECT
// undefined ==>when we used the value undefined , it returns undefined


// Numbers:
let length = 16;
let weight = 7.5;

// Strings:
let color = "Yellow";
let lastName = "Johnson";

// Booleans
let x = true;
let y = false;

// Object:
const person = {firstName:"John", lastName:"Doe"};

// Array object:
const cars = ["Saab", "Volvo", "BMW"];

// Date object:
const date = new Date("2022-03-25");

//Non Primitive DataType
//object

// https://tc39.es/ecma262/
// https://developer.mozilla.org/en-US/docs/Web/API/Window/alert

